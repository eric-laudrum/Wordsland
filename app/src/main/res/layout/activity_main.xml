<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:background="@color/darker_green"
    >


    <!-- HEADER -->
    <LinearLayout
        android:id="@+id/header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"

        android:orientation="horizontal"
        android:background="@color/darker_green"

        android:paddingTop="20dp"

        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        >

        <!-- TITLE -->
        <EditText
            android:id="@+id/title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"

            android:text="Wordsland"
            android:textColor="@color/light_green"
            android:textSize="40sp"

            android:layout_marginTop="25dp"
            />

        <!-- HEADER VALUES (round & tiles left) -->
        <LinearLayout
            android:id="@+id/header_values"
            android:layout_width="194dp"
            android:layout_height="match_parent"
            android:orientation="vertical"


            >

            <!-- ROUND / LEVEL NUMBER -->
            <TextView
                android:id="@+id/round_counter_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"

                android:layout_gravity="center"
                android:layout_marginTop="16dp"
                android:textColor="@color/light_green"

                android:textSize="20sp"
                android:textStyle="bold"
                tools:text="Round: 1" />
            <!-- TILES LEFT IN BAG -->
            <!-- TODO: turn this into a bag icon with a number badge-->
            <TextView
                android:id="@+id/tiles_left_counter_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"

                android:layout_gravity="center"
                android:layout_marginTop="16dp"
                android:text="LOADING..."
                android:textColor="@color/light_green"
                android:textSize="20sp"
                android:textStyle="bold"
                tools:text="Tiles: 188" />

        </LinearLayout>

    </LinearLayout>


    <!-- Main Grid -->
    <androidx.gridlayout.widget.GridLayout
        android:id="@+id/grid"
        android:layout_width="0dp"
        android:layout_height="0dp"

        android:background="@color/darker_green"

        app:layout_constraintTop_toBottomOf="@+id/header"
        app:layout_constraintBottom_toTopOf="@+id/letter_tray_recycler_view"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        />

    <!-- Player Tiles -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/letter_tray_recycler_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:overScrollMode="never"

        android:background="@color/green"

        android:clipToPadding="false"
        android:layout_marginBottom="10dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/button_bar"
        />

    <LinearLayout
        android:id="@+id/button_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"

        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"

        android:orientation="horizontal"
        android:gravity="center"
        android:background="@color/green"
        android:paddingBottom="25dp"

        >

        <Button
            android:id="@+id/enter_word_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"

            android:text="Enter"
            android:textColor="@color/darker_green"
            android:backgroundTint="@color/light_green"
            />

        <Button
            android:id="@+id/recall_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Recall"
            android:textColor="@color/darker_green"
            android:backgroundTint="@color/light_green"
            />

        <Button
            android:id="@+id/swap_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Swap"
            android:textColor="@color/darker_green"
            android:backgroundTint="@color/light_green"
            />


    </LinearLayout>



</androidx.constraintlayout.widget.ConstraintLayout>